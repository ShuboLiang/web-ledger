# 轻账 Backlog

基于 2026-08-21 的代码盘点整理。工作量以「人天」估算，假设由熟悉本仓库的开发者完成，含自测但不含大规模回归。

优先级含义：

- **P0** — 数据安全或防灾问题，应排在所有新功能之前。
- **P1** — 高价值功能缺口，直接影响用户能否长期使用。
- **P2** — 体验层缺失，影响留存和观感。
- **P3** — 工程质量，不影响用户但影响后续迭代速度。
- **P4** — 已知小问题与打磨项。

---

## P0 数据安全与防灾

### ~~P0-1 移除 `compose.yaml` 中的明文数据库密码~~ — ✅ 已完成（2026-08-21）

`compose.yaml` 已改为从 `.env` 读取 `${POSTGRES_PASSWORD}`；新增 `.env.example`；本地 `.env` 已轮换密码并同步 `ALTER USER`。Git 历史中的旧密码仍视为泄露，勿再使用。

### P0-2 自动定时备份与保留策略 — 1.5 人天

当前备份依赖人工执行 `docker compose run --rm backup`，而 `pgdata` 是单机 bind mount，没有任何冗余。记账数据属于丢失后无法重建的类型。

改动：compose 增加一个常驻备份服务（cron 或 `sleep` 循环）执行 `pg_dump -Fc`；实现按天保留 N 份的轮转清理；备份失败时写明显日志。建议同时提供一条已验证的 `pg_restore` 恢复命令文档。

### P0-3 认证接口限流 — 0.5 人天

`src/modules/auth/` 无任何速率限制，登录与注册可被暴力尝试。密码用 scrypt（`auth.service.ts:52-68`）本身有计算成本，但不能替代限流。

改动：引入 `@nestjs/throttler`，对 `/api/auth/login` 与 `/api/auth/register` 单独设较严阈值，其余 API 设宽松全局阈值。

### P0-4 修改密码 + 会话管理 — 2 人天

`功能说明.md` 第 15 节已列为未实现。`User.tokenVersion` 字段在 schema 中存在，但全仓库没有任何递增逻辑——这个字段的设计用途正是「改密后吊销全部旧会话」，属于预留未接。

改动：新增 `POST /api/auth/password`（校验旧密码 → 更新哈希 → `tokenVersion + 1`）；`auth.guard.ts:46-52` 已有版本比对逻辑，无需改动即可生效。前端在设置页「安全与登录」区块加表单，并列出 `AuthSession` 记录支持单个吊销。

---

## P1 高价值功能缺口

### P1-1 账单导入（支付宝 / 微信 / 银行 CSV） — 5～8 人天

目前所有账目只能手敲或让 AI 逐条录入，这是记账类应用用户流失的首要原因。本项目有一个别处没有的优势：Pi Agent 已具备结构化的 `ledger_propose_create` 批量提案与待确认区，导入流程的「后半段」基本是现成的。

改动：后端新增导入端点与各渠道的 CSV 解析适配器（支付宝/微信导出格式差异较大，需分别处理编码、表头偏移、退款行）；去重策略（按日期+金额+对方账户指纹）；接入现有 proposal 机制做 AI 归类；前端新增上传与批量审核界面。

建议先只做支付宝一个渠道跑通全链路，再扩展。

### P1-2 定期账单 / 订阅记账 — ✅ 已完成

房租、话费、会员、房贷等固定支出每期需重复录入。已新增 `RecurringRule` / `RecurringGeneration`，支持按日/周/月/年生成、重启补齐、自动入账或确认后入账，以及前端管理与到期提醒。

~~改动：新增 `RecurringRule` 表（频率、起止、模板字段、下次生成日）；后端定时任务按日扫描生成账目（需保证重启后补齐、且不重复生成）；前端管理界面 + 生成前提醒。~~

### P1-3 回收站 / 软删除 — ✅ 已完成

删除改为软删除（`deletedAt`），列表与统计默认排除已删记录；回收站支持恢复与彻底删除，超过 30 天自动清理。

~~改动：`Transaction` 增加 `deletedAt`；删除改为标记；所有查询加过滤条件（需逐一排查 `ledger.service.ts` 中的查询）；新增回收站页支持恢复与彻底删除；加定期清理超过 30 天的记录。~~

性价比高于「审计日志浏览页」，建议先做这个。

### P1-4 收入侧能力补齐 — 2 人天

能记收入，但分析能力全部围绕支出：预算只管支出上限、`/heatmap` 只画支出（写死 `direction=expense`）、标签分析接口已返回 `income` 字段而界面未展示。

改动：热力图加收支切换；标签页展示收入统计；分析页增加收支对比与收入结构视图。

### P1-5 人情往来 / 应收应付 — ✅ 已完成

日常「朋友先垫付」「公司待报销」「别人替我付」有了独立承载，`/lending` 页按人看净额、按笔看结清。

~~改动：可复用 `Account` 体系新增「往来」账户类型，或新增独立的应收应付模型。建议先评估哪种方式对现有净资产计算侵入更小。~~

最终采用两者结合：往来对象复用 `Account`（新增 `contact` 类型），直接继承余额、转账与净资产计算；另建 `LendingEntry` / `LendingSettlement` 记录每一笔往来与每一次结算，支持约定还款日、逾期提醒、部分结清与按人 FIFO 冲抵。资产负债页把应收应付与可用资产、总负债分开列示，往来产生的资金移动禁止在资产页直接撤销。AI 侧新增查询往来、记一笔往来和结算三个工具。

### P1-6 小票 / 照片附件 — 3～4 人天

报销、大额消费、保修凭证均需要。schema 无任何 attachment 字段。

改动：需引入文件存储（本地目录挂载已有 `DATA_DIR` 可复用）；上传端点与大小/类型校验；缩略图；账目表单与详情的展示；备份策略要同步覆盖附件目录（与 P0-2 有依赖）。

### P1-7 多账本创建与切换 — 3 人天

已在 `功能说明.md` 第 15 节列出。`Ledger.userId` 当前是 `@unique`，即每用户恰好一个账本，解除这个约束会影响 `CurrentUserService.ledgerId`（`current-user.service.ts:20-22`）这一全局数据隔离入口。

改动面广但改动点集中，属于「一处放开、全局跟随」的类型。

---

## P2 体验层

### ~~P2-1 PWA 支持~~ — ✅ 已完成（2026-08-22）

已接入 `vite-plugin-pwa`，提供可安装 Manifest、Chromium 与 iOS 兼容图标、自动更新 Service Worker、应用外壳预缓存和离线导航回退；用户 API 明确不进入缓存。

~~改动：接入 `vite-plugin-pwa`，配置 manifest、图标集与 service worker 缓存策略。~~

### P2-2 暗色模式 — 3 人天

主题写死 `theme.defaultAlgorithm`（`main.tsx`）。切换 Ant Design 的 `darkAlgorithm` 本身很简单，真正的工作量在 `apps/web/src/styles.css`——3887 行、约 100 处硬编码颜色字面量需要改为 CSS 变量或设计令牌。

建议：先把 `styles.css` 的颜色收敛到变量，再接主题切换，否则会反复返工。

### P2-3 提醒与通知 — 2～3 人天

预算超支目前只有打开页面才看得到内联 Alert，没有任何主动提醒。这直接关系到用户能否养成记账习惯。

改动：依赖 P2-1 的 service worker 做 Web Push，或先做更轻的方案（每日未记账时在应用内提示）。建议排在 PWA 之后。

### P2-4 项目（Project）管理界面 — 1 人天

`功能说明.md` 已列出重命名/合并/删除未实现。当前 Project 仅作为账目表单的 `<datalist>` 联想词典，且与 `Transaction` 之间**没有外键关联**（创建账目时按 `item` 文本 upsert，见 `ledger.service.ts:1483-1487`）。

注意：做重命名/合并前需要先决定是否补上外键关联，否则改名无法同步历史账目。这个决策会影响工作量，可能从 1 人天涨到 2～3 人天。

### P2-5 全量数据导出 / 导入 — 2 人天

目前仅账目页有客户端 CSV 导出，无 JSON 全量导出，用户无法自主迁移数据。设置页「数据导入导出」为规划中占位。

---

## P3 工程质量

### P3-1 核心金额逻辑的单元测试 — 2～3 人天

全仓库零个 `*.spec.ts`，测试只有 `tests/smoke.mjs`（约 1400 行 E2E）。这个 smoke 覆盖面意外地广，但它是全有或全无的：任何一处失败就整体红，定位困难。

建议引入 Vitest，优先覆盖 `ledger.service.ts`（1876 行，仓库最大的业务文件）中的可用额度推算、还款拆分（本金/利息/手续费）、预算滚动计算。

### P3-2 接上 DTO 校验 — 1 人天

`src/modules/transactions/transaction.dto.ts` 已定义 `TransactionInputDto` 但 controller 未使用；全局 ValidationPipe 的 `whitelist` 为 `false`（`main.ts:13`），多数端点接收 `Record<string, unknown>` 后在 service 层手工校验。DTO 已经写好，接上即可。

开启 `whitelist: true` 需要回归各端点，注意可能打断现有请求体中的额外字段。

### P3-3 结构化日志与错误上报 — 1～2 人天

当前仅 NestJS 默认 Logger，无请求 ID、无错误聚合。生产环境出问题只能翻容器日志。

### P3-4 审计日志浏览界面 — 1.5 人天

`AuditLog` 表写入完整（交易、账户、转账、还款、预算、分类等均有记录），但只写不读，无任何查询 API 或界面。

---

## P4 已知小问题

来自 `TODO.md` 及本次盘点：

1. 设置页菜单切换无效 —— `pages/settings.tsx` 中 `section` 状态会更新，但右侧始终渲染 `AiModelSettings`。补其他区块前必须先修。（0.2 人天）

2. 手机端标签需要左右滑动，操作不便。（0.5 人天）
3. Dashboard 的 AI 快捷记账直接写入不需确认，与 `/ai` 页必须确认的行为不一致，建议统一。（0.3 人天）
4. `frontend.controller.ts` 未列出 `heatmap` 路径，生产环境直接刷新该页可能 404。（0.1 人天）

---

## 建议的执行顺序

1. **先清 P0**（约 4.5 人天）——密码轮换和自动备份是防灾，不做的话后面所有功能都建立在有风险的基础上。
2. **再做 P2-1 PWA + P1-1 账单导入**——前者 1 人天见效，后者是本项目最大的用户价值缺口且能复用已有的 AI 提案架构。
3. **P1-2 / P1-3 / P1-5 已完成**——可继续 P1-4 收入侧能力或其它高价值缺口。
4. **P3-1 单元测试穿插进行**——在改 `ledger.service.ts` 之前补上金额逻辑的测试，能显著降低后续改动风险。
