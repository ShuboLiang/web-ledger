# 复制为 .env 后填写真实值。切勿将 .env 提交到 Git。
POSTGRES_DB=qingzhang
POSTGRES_USER=qingzhang
POSTGRES_PASSWORD=change-me

# 本地开发（npm run dev）连接宿主机上的 Postgres。
# Docker Compose 中的 web 服务会用 POSTGRES_* 自行拼出容器内连接串，不读这一项。
DATABASE_URL=postgresql://qingzhang:change-me@localhost:5432/qingzhang
