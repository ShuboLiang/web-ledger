# 轻账 Web 完整功能说明

> 文档对应版本：`2.0.0`
>
> 更新日期：2026-08-12
> 项目形态：React 网页端 + NestJS API + PostgreSQL + Pi SDK Agent + Docker Compose

## 1. 产品概述

轻账是一套本地优先、支持 AI 自然语言操作的网页记账系统。系统将账目、统计、分类账户管理、AI 助手和模型配置拆分为独立页面，适合长期保存大量账目并按日、周、月、年或自定义日期范围查看财务数据。

核心原则：

- 数据保存在本机 PostgreSQL，不依赖浏览器本地存储作为账本数据库。
- 支出在数据库中保存为负数，收入保存为正数；页面输入金额时统一填写正数并选择收支类型。
- 一级分类和二级分类形成级联关系。
- AI 对账本的新增、修改和删除只能生成待确认操作，用户确认后才写入数据库。
- Pi SDK 仅作为 Agent 驱动内核，网页不要求用户理解 Pi 的内部实现。
- Docker 镜像与数据库、AI 配置分离，更新容器不删除业务数据。
- 所有业务接口均要求登录，账目、分类、预算和 AI 数据按用户隔离。

## 2. 页面与导航

| 页面      | 路径                  | 主要用途                                         |
| --------- | --------------------- | ------------------------------------------------ |
| 财务概览  | `/dashboard`          | 查看本月核心指标、趋势、分类占比和最近账目       |
| 账目明细  | `/transactions`       | 查询、筛选、新增、编辑、删除、批量分类和导出账目 |
| 统计分析  | `/analytics`          | 按日、周、月、年和自定义范围分析收支             |
| 预算规划  | `/budgets`            | 管理月度总预算和一级分类预算，查看使用率与预警   |
| 定期账单  | `/recurring`          | 管理房租、会员等固定收支并按期生成账目           |
| 回收站    | `/trash`              | 恢复或彻底删除误删的账目                         |
| 资产负债  | `/finance`            | 管理账户额度、转账、贷款放款和还款               |
| 人情往来  | `/lending`            | 记录垫付、代付、借入，按笔跟踪谁欠我、我欠谁     |
| 消费标签  | `/tags`               | 管理标签并按场景、人物或目的查看消费             |
| AI 助手   | `/ai`                 | 使用自然语言查账、记账、改账、删账和分析消费     |
| 分类管理  | `/management`         | 管理一级分类和二级分类                           |
| 注册/登录 | `/register`、`/login` | 创建个人账户并进入长期有效的登录会话             |
| 系统设置  | `/settings`           | 配置当前用户的 AI 模型；展示其他设置模块入口     |
| 更多功能  | `/more`               | 手机端集中访问 AI、预算、分类和设置              |

桌面端使用左侧导航和顶部操作栏，预算拥有独立导航入口。移动端底部固定为“概览、账目、记一笔、分析、更多”：高频功能直接到达，AI、预算、分类和设置统一收纳在“更多”页面。表格在移动端会切换成账目卡片；AI 页面通过历史对话抽屉支持新建、切换和删除会话。

全局顶部提供：

- 跳转到账目搜索。
- 跳转到 AI 助手快速记账。
- 当前登录用户入口和退出登录。
- 当前日期显示。
- 随时打开“记一笔”抽屉。
- 进入系统设置。

### 2.1 账户与登录

- 用户名密码注册和登录；用户名全局唯一，密码使用带随机盐的 `scrypt` 哈希保存，不保存明文。
- 原始会话 Token 只放在 `HttpOnly` Cookie 中，前端 JavaScript 无法读取；数据库只保存 Token 的 SHA-256 摘要。
- 服务端会话不设置到期时间，浏览器 Cookie 使用十年有效期。只要用户不主动退出、不清除 Cookie，且服务端会话仍存在，就不要求重复登录。
- 退出登录会立即吊销当前服务端会话，即使旧 Token 被复制也不能再次使用。
- 每个用户拥有独立账本，账目、分类、预算、模型配置和 AI 对话互不可见。
- 从无账户旧版本升级时，第一个注册账户会接管原有账本、AI 模型配置和对话；后续账户会创建空白个人账本与默认分类。

## 3. 财务概览

财务概览默认以数据库中最新一笔账目的日期作为统计锚点，而不是强制使用系统当天日期。

### 3.1 核心指标

- 本月支出。
- 本月收入。
- 本月结余。
- 本月日均支出。
- 点击指标卡片可跳转到对应月份的账目明细。

### 3.2 图表和信息

- 近 14 日支出趋势折线图。
- 本月一级分类支出占比环形图。
- 最近 6 笔账目。
- 本月最大支出类别提示。
- 人情往来逾期与临近到期提醒，可一键进入往来页处理。
- 跳转到完整统计分析。
- 跳转到 AI 助手继续询问消费情况。

## 4. 账目管理

### 4.1 新增账目

“记一笔”使用右侧抽屉，不遮挡原账目工作区。可填写：

- 日期，默认使用亚洲上海时区的当天日期。
- 类型：支出或收入。
- 金额，必须大于 `0`，支持两位小数。
- 项目，最多 80 个字符。
- 一级分类。
- 二级分类。
- 账户，可清空；若已设置默认付款账户则预选它，否则默认不记账户。
- 备注，最多 500 个字符。

不选账户时，这笔账仍计入收支、分类、预算和标签，但不会改变任何账户余额。适合别人代付、记不清付款账户、或不想维护钱包余额的消费。

保存后系统会：

- 将支出转换为负数、收入转换为正数写入 PostgreSQL。
- 自动刷新概览、统计和账目列表。
- 自动把新项目加入项目字典并去重。
- 自动把新的“一级分类 + 二级分类”组合加入分类字典并去重。
- 写入审计日志。

### 4.2 分类级联选择

- 一级分类使用真实下拉框。
- 选择一级分类后，二级分类只显示其直接下级。
- 切换一级分类时自动选择该一级分类下的第一个二级分类。
- 停用分类不会出现在新账目的下拉框中。
- 编辑使用停用分类的旧账目时，仍可看到原分类并标记为“已停用”。
- 没有任何可用分类时，页面会提示先到“分类与账户”新增分类。

### 4.3 编辑和删除

- 点击桌面表格中的编辑按钮打开编辑抽屉。
- 移动端点击账目卡片进入编辑。
- 可修改日期、收支类型、金额、项目、分类、账户和备注；也可以把已有账目改为不记账户。
- 删除账目会移入回收站，可在 30 天内恢复；确认提示会说明这一点。
- 编辑和删除完成后自动刷新相关统计。

### 4.4 查询和筛选

支持以下条件组合筛选：

- 月份。
- 账户，包含「不记账户」。
- 收支类型。
- 一级分类。
- 二级分类。
- 项目、备注或分类关键词。

筛选条件保存在 URL 查询参数中，因此可以刷新页面或复制当前查询地址。改变筛选条件后自动回到第一页。

### 4.5 排序、分页和大数据量

- 账目分页由服务端处理，不会一次加载一万条记录到浏览器。
- 默认每页 20 笔。
- 支持上一页和下一页。
- 显示总笔数、当前页和总页数。
- 支持按日期、金额和项目排序。
- 服务端单页最大 100 笔。
- 数据库建立日期和分类索引，适合长期账目查询。

### 4.6 批量分类

- 支持勾选当前页单笔或多笔账目。
- 支持全选当前页。
- 可批量修改一级分类和二级分类。
- 批量分类会自动创建或重新启用目标分类字典项。
- 操作结果记录到审计日志。

### 4.7 筛选保存和列设置

- 可把当前筛选条件保存在当前浏览器中。
- 可一键读取已保存的筛选条件。
- 可控制日期、项目、分类、账户、备注和金额列是否显示。

保存的筛选条件属于浏览器本地偏好，不属于 PostgreSQL 业务数据。

### 4.8 CSV 导出

- 导出当前全部筛选结果，而不只是当前页。
- 后台按页读取完整结果，避免单次接口返回无限数据。
- 导出字段包括日期、项目、一级分类、二级分类、账户、备注和金额。
- 文件使用带 BOM 的 UTF-8 CSV，便于 Excel 正确识别中文。

## 4.9 定期账单

路径：`/recurring`

用于房租、会员、话费等固定收支，按设定周期自动或确认后生成账目。

- 频率支持每天、每周、每月、每年，并可设置间隔（例如每 2 个月）。
- 可设置开始日期、可选结束日期、付款账户、分类与备注。
- 默认到期自动入账；也可关闭「到期自动入账」，改为先出现待确认项，确认后再写入账本。
- 服务启动时与每小时会扫描到期规则：补齐停机期间漏掉的周期，并通过「规则 + 生成日」唯一约束避免重复入账。
- 支持立即生成本期、跳过本期、暂停/启用和删除规则；删除规则不影响已经生成的账目。
- 页面展示 7 日内到期提醒与待确认列表。

### 4.10 回收站

路径：`/trash`

- 删除账目时写入 `deletedAt`，不立即从数据库移除。
- 普通列表、统计、预算、标签、账户余额等计算均排除已删除账目。
- 回收站可按关键词搜索，支持单笔/批量恢复，以及所选或全部彻底删除。
- 超过 30 天未恢复的记录由服务端定期彻底清理。

## 5. 统计分析

### 5.1 统计周期

支持：

- 日。
- 周，固定为周一至周日。
- 月。
- 年。
- 自定义开始日期和结束日期。

可以：

- 使用左右箭头查看上一周期和下一周期。
- 直接选择统计锚点日期。
- 清楚查看当前统计周期的开始与结束日期。
- 退出自定义范围后恢复周期统计。

单次自定义统计范围最长为 10 年。记录十年账目后，可以使用年统计或自定义范围查看历史数据。

### 5.2 统计指标

- 周期支出。
- 周期收入。
- 周期结余数据由后端计算并供概览使用。
- 与上一同长度周期的变化比例。
- 无上一周期数据时不虚构变化率。

### 5.3 趋势图

- 日视角提供近 14 日序列。
- 周视角提供近 8 周序列。
- 月视角提供近 12 月序列。
- 年视角按月展示。
- 自定义范围不超过 120 天时按日展示，超过 120 天时按月展示。

### 5.4 分类分析

- 一级分类支出占比饼图。
- 二级分类支出占比饼图。
- 二级分类图例包含所属一级分类，避免同名二级分类混淆。
- 分类支出金额和占比排行。
- 饼图支持悬浮查看金额和占比。

### 5.5 其他分析

- 每日支出日历：桌面端和手机端均按月切换，颜色深浅表示当天支出，点击日期可查看当天账目；自定义范围跨月或跨年时，趋势图展示整体变化，日历用于逐月钻取。
- 当前月份大额支出列表。
- 分类变化排行。
- 日、周、月、年与前一周期的环比结果。

## 6. AI 助手

### 6.1 Agent 工作方式

AI 通过 Pi SDK 驱动，Pi SDK 负责模型会话、工具调用和上下文管理。模型本身由用户在“设置 → AI 模型”中配置，系统不提供环境默认模型。

AI 可处理：

- 自然语言新增一笔或多笔账目。
- 用户未指定账户时，若已设置默认付款账户则使用它，否则不记账户；明确说不记账户、别人请客或只记消费时，账目不绑定付款账户。
- 根据上下文理解“它”“那笔”“刚才那条”等多轮指代。
- 查询最近账目。
- 查询日、周、月、年支出汇总。
- 查询一级分类和二级分类占比。
- 查询已有项目、分类、账户和标签字典。
- 提议修改指定账目。
- 提议删除指定账目。
- 查询账户可用额度、负债和净资产。
- 提议新增或修改账户、账户转账、还款、额度校准和撤销资金移动。
- 查询标签消费，并提议新增、修改、停用或删除标签。
- 回答消费和账本相关问题。
- 可在“设置 → AI 模型”中填写助手提示词，补充个人记账习惯；该提示词追加在内置规则之后，不能覆盖工具流程。

### 6.2 内置账本工具

| 工具                                       | 功能                             | 是否直接写库     |
| ------------------------------------------ | -------------------------------- | ---------------- |
| `ledger_search_transactions`               | 按关键词、日期、金额或分类搜索账目 | 否               |
| `ledger_get_transaction`                   | 按编号读取单笔账目               | 否               |
| `ledger_list_transactions`                 | 查询最近账目并定位编号           | 否               |
| `ledger_get_summary`                       | 查询日、周、月、年汇总与分类占比 | 否               |
| `ledger_list_dictionaries`                 | 查询项目、分类、付款账户和标签   | 否               |
| `ledger_propose_create`                    | 生成新增账目建议                 | 否，进入待确认区 |
| `ledger_propose_update`                    | 生成账目修改建议                 | 否，进入待确认区 |
| `ledger_propose_delete`                    | 生成账目删除建议                 | 否，进入待确认区 |
| `ledger_propose_category_icon_update`      | 生成修改分类图标建议             | 否，进入待确认区 |
| `ledger_get_finance_overview`              | 查询账户、可用额度与近期资金移动 | 否               |
| `ledger_propose_account_create`            | 生成新增账户建议                 | 否，进入待确认区 |
| `ledger_propose_account_update`            | 生成账户改名、改期初、设默认或启停建议 | 否，进入待确认区 |
| `ledger_propose_account_reconcile`         | 生成账户余额/额度校准建议        | 否，进入待确认区 |
| `ledger_propose_account_delete`            | 生成删除未使用账户建议           | 否，进入待确认区 |
| `ledger_get_tag_overview`                  | 查询月份标签概览与金额           | 否               |
| `ledger_get_tag_analytics`                 | 查询单个标签消费详情             | 否               |
| `ledger_propose_tag_create`                | 生成新增标签建议                 | 否，进入待确认区 |
| `ledger_propose_tag_update`                | 生成修改或启停标签建议           | 否，进入待确认区 |
| `ledger_propose_tag_delete`                | 生成删除标签建议                 | 否，进入待确认区 |
| `ledger_propose_transfer`                  | 生成账户转账建议                 | 否，进入待确认区 |
| `ledger_propose_transfer_reverse`          | 生成撤销账户转账建议             | 否，进入待确认区 |
| `ledger_propose_adjustment_reverse`        | 生成撤销额度调整建议             | 否，进入待确认区 |
| `ledger_propose_repayment`                 | 生成本金转账加利息支出的还款建议 | 否，进入待确认区 |
| `ledger_get_lending_overview`              | 查询人情往来对象、净额与待结清明细 | 否               |
| `ledger_propose_lending_entry`             | 生成记一笔垫付、代付或借入的建议 | 否，进入待确认区 |
| `ledger_propose_lending_settle`            | 生成收款或还钱的结算建议         | 否，进入待确认区 |
| `ledger_propose_discard`                   | 从待确认区移除尚未写入的建议     | 否               |

### 6.3 待确认机制

- AI 的账目、账户、转账、还款和额度调整变更不会直接写入账本。
- 建议会显示在右侧“待确认操作”区域。
- 用户可以全部取消。
- 只有点击“确认执行”后，后端才执行账目变更。
- 执行成功后会刷新账目和统计。
- 确认或取消结果会反馈给当前多轮 Agent，供下一轮理解上下文。

### 6.4 多轮对话

- 支持在同一会话中连续追问。
- 支持新建、切换和删除历史会话。
- 同一会话正在生成回答时会阻止重复并发发送。
- 会话、消息、待确认操作和自动生成的会话标题均持久化到 PostgreSQL。
- 刷新网页或重启服务后，历史消息和未处理的待确认操作仍会恢复。
- Pi SDK 的内存会话释放或服务重启后，会从数据库注入最近的历史消息，继续理解多轮上下文。
- 确认执行时以后端持久化的待确认操作为准，不信任浏览器重新提交的操作内容。

### 6.5 Markdown 回答

AI 回答支持 Markdown 和 GitHub Flavored Markdown，包括：

- 标题和段落。
- 有序和无序列表。
- 表格。
- 代码和代码块。
- 链接。
- 加粗和斜体。

## 7. AI 模型配置

### 7.1 自定义配置列表

- 新增多套模型配置。
- 编辑已有模型配置。
- 切换默认模型。
- 删除非默认模型配置。
- 默认模型不能直接删除，必须先切换默认配置。
- AI 页面可直接切换当前默认模型；新会话使用新模型。
- 不使用系统环境默认模型，必须由用户自行创建配置。

### 7.2 支持的配置项

- 配置名称。
- Provider ID。
- Model ID。
- Base URL。
- API Key。
- 是否发送认证请求头。
- 是否启用模型思考，以及极简、低、中、高、很高、最高六个思考等级。
- 接口协议。
- 用户级助手提示词：补充记账习惯和回答风格，追加在内置系统提示之后，不能覆盖工具与安全规则。

支持的协议：

- OpenAI Chat Completions。
- OpenAI Responses。
- Anthropic Messages。
- Google Generative AI。

可连接兼容上述协议的云端服务或本地模型服务。无密钥的本地服务仍需填写占位值以完成配置。

### 7.3 配置保存规则

- 模型配置和 API Key 保存在 PostgreSQL。
- API 查询只返回 `hasApiKey` 状态，不会把完整密钥返回浏览器。
- Pi SDK 所需的 `pi-models-<用户ID>.json` 在运行时写入 `/app/data`，不同用户互不覆盖。
- 用户模型运行文件使用仅所有者可读写的文件权限。
- “保存配置”只保存表单内容，不会偷偷切换当前默认模型。
- 已保存的非默认配置可通过独立的“设为默认”操作启用；首个模型配置会自动成为默认配置。
- 修改当前默认模型或切换默认模型会释放 Pi SDK 的内存会话；下一轮会依据数据库聊天记录恢复上下文，并使用新配置。

注意：当前 API Key 在数据库中没有额外进行应用层加密，应通过宿主机权限、数据库访问控制和备份文件权限保护。

## 8. 分类、账户和基础资料管理

### 8.1 分类管理

分类卡片显示：

- 一级分类名称。
- 下属二级分类数量。
- 一级分类关联的账目总数。
- 每个二级分类关联的账目数量。
- 启用、停用或已合并状态。
- 已合并分类的目标分类。

支持的操作：

- 新增一级分类和二级分类组合。
- 重命名二级分类。
- 把二级分类移动到另一个一级分类。
- 合并二级分类。
- 停用和重新启用分类。
- 重命名整个一级分类。
- 安全永久删除二级分类。
- 安全永久删除一级分类及其全部无引用子分类。

### 8.2 分类历史数据保护

分类采用“分类外键 + 账目分类文字快照”设计：

- 每笔账目通过 `categoryId` 关联分类。
- 每笔账目同时保留一级分类和二级分类文字，便于历史展示和查询。
- 分类外键使用数据库 `RESTRICT` 删除规则，防止绕过网页误删正在使用的分类。
- 重命名和移动会在同一个数据库事务中同步更新分类及所有历史账目。
- 一级分类重命名还会同步更新相关预算。
- 合并会把源分类的历史账目转移到目标分类，并把源分类归档停用。
- 合并到已停用目标时会自动启用目标分类。
- 有账目引用的分类禁止永久删除。
- 一级分类仍有关联账目或预算时禁止永久删除。
- 停用分类不会修改历史账目和历史统计。

推荐日常使用顺序：优先停用，重复分类使用合并，只有无引用分类才永久删除。

### 8.3 账户管理

- “资产与负债”页面新增账户并显示实时可用额度。
- 账户名称去重。
- 支持现金、银行卡、电子钱包、信用账户和贷款账户类型，均可独立创建。
- 设置期初可用额度与起算日；只计算起算日之后且不晚于今天的账单、转账和校准记录。
- 可设置普通记账的默认付款账户，也可以不设置；信用账户可以设为默认，贷款账户不行。未设置时新账默认不记账户。
- 每个账户提供明确的编辑入口，可改名、改正期初可用额度、设为或取消默认、启停或删除未使用账户。
- 新账目可以不选账户；不选时只记消费，不改变账户余额。可选账户限于启用中的现金、银行卡、电子钱包或信用账户；贷款账户通过转账放款和还款。
- 资产与负债页会提示本月未记账户的账目笔数；这些消费会计入收支，因此账户合计与消费合计可能对不上。
- 任意启用账户之间可自由转账和撤销；转账只改变两个账户额度，不进入收入和支出统计。贷款放到银行卡、用银行卡还花呗都记转账。
- 支持按实际可用额度校准；信用账户提额、降额走校准，差额不计入收入和支出。额度调整会出现在资金移动列表中，可像转账一样撤销。
- 可用额度按“期初可用额度 + 日常收支 + 转入 - 转出 + 校准差额”计算，未来流水不会提前改变今日额度。
- 零余额且没有账单、转账或校准记录的账户可永久删除，包括当前默认付款账户；其他账户只能停用。
- 停用保留额度、历史和净资产统计，但不能继续记账、转账、还款或校准。

### 8.3.1 还款与放款

- 还款采用“转账 + 支出”双轨：本金从付款账户转到信用/贷款账户，不计入支出；利息和手续费单独记为财务费用支出。
- 贷款账户独立创建，期初可用额度一般为 0；放款时从贷款账户转到银行卡，贷款账户额度下降、银行卡增加。
- 还款反向转账使贷款或信用账户额度回升。
- 页面显示可用资产、总负债、净资产、账户可用额度和最近资金移动。
- 总负债只统计信用/贷款账户当前为负的部分；信用账户保持正可用额度时不计入总负债。
- 手机端使用两列摘要和单列账户卡片，所有新增、转账、还款与校准操作使用移动端抽屉完成。

### 8.3.2 消费标签

- 一笔账可关联最多 8 个标签；分类表示资金去向，标签表示场景、人物或目的。
- 支持标签新增、查询、改名、换色、启停和删除；删除标签只解除账目关联，不删除账目。
- 标签页可按月或按年显示标签支出、相关账目数、平均每笔、消费节奏、分类去向和账单明细；年度消费节奏按 12 个月汇总。
- 账目页支持显示、搜索和筛选标签，CSV 导出包含标签名称。
- 同一笔账在每个标签分析中均会显示，但账本总支出仍只计算一次；不同标签的金额不应直接相加。
- 手机端标签横向滑动，摘要采用两列布局，编辑使用底部抽屉。

### 8.3.3 人情往来

- 独立页面 `/lending` 管理跟具体某个人、公司或团体之间的欠款，既看每个人的净额，也按笔跟踪每一次垫付和结清。
- 每个往来对象是一个“人情往来”类型的账户，因此欠款自动进入资产负债与净资产统计，但不混进可用资产和总负债：别人欠我记入应收，我欠别人记入应付，净资产按“可用资产 + 应收 − 总负债 − 应付”计算。
- 三种记法覆盖日常场景：
  - 我先垫付：金额按账单原样填“这次一共付了多少”，其中“我自己那份”记成一笔正常支出并计入分类和预算，剩下的挂成对方欠我的，不算我的支出。自己那份必须小于总金额。
  - 别人替我付：记成我的一笔支出（需要分类），同时挂上我欠对方的钱，还钱时自动冲抵，不会重复计支出。
  - 我借入现金：钱进入我的账户，余额增加但不计入收入。
- 可以填约定还款日，逾期和临近到期会在财务概览和往来页顶部提醒。
- 结算支持两种粒度：指定某一笔按笔结清（可只结一部分），或按人整体结算，此时金额从最早的一笔开始依次冲抵；双向都有未结清时必须指定结算方向，金额也不能超过待结清总额。
- 结算只在自己的资金账户和往来对象之间搬钱，不产生收入或支出。
- 往来产生的资金移动会在资产负债页标记为不可直接撤销，必须回到往来页撤销对应记录，撤销时会连带撤销资金移动，关联账目进入回收站可再恢复。
- 有历史往来记录的对象不能直接删除，避免静默丢失欠款。
- 记账抽屉的付款账户下拉把“我的账户”和“人情往来（对方替我付）”分组显示，方便直接把某笔消费记成别人代付。
- AI 助手可以直接查询往来概览、建议记一笔往来和建议结算，走同样的待确认流程。

### 8.4 项目字典

- 项目字典作为后台自动学习能力保留，不再提供单独的手工管理入口。
- 手动记账时提供已有项目输入建议。
- 新账目使用新项目时自动加入字典。
- AI 可以读取项目字典辅助理解历史记账习惯。

### 8.5 预算

- 预算使用独立页面 `/budgets`，不再放在分类管理内部。
- 每个月可设置一个总预算，并可选设置各一级分类预算。
- 同一月份、同一预算范围禁止重复创建；总预算也只能有一个。
- 显示预算金额、已花、剩余、使用率和进度条。
- 使用率达到 80% 时显示预警，超过 100% 时标红提示超支。
- 支持编辑和删除预算。
- 支持将上月预算复制到当前月份，并自动跳过当前月已经存在的预算范围。
- 概览页显示本月总预算进度，以及已设置的分类预算、使用率和风险状态，并可进入预算管理页。
- 一级分类重命名时同步更新预算分类名称。

### 8.6 标签

- 新增标签。
- 设置标签颜色。
- 启用和停用标签。

当前标签尚未绑定到账目，也未提供重命名和永久删除。

## 9. 设置模块状态

| 设置模块     | 当前状态                                                                     |
| ------------ | ---------------------------------------------------------------------------- |
| AI 模型      | 已实现完整配置列表、编辑、切换默认和删除非默认配置                           |
| 个人设置     | 页面入口已完成，后端功能待实现                                               |
| 账本设置     | 页面入口已完成，多账本管理待实现                                             |
| 安全与登录   | 已实现注册、登录、长期会话、退出吊销和用户数据隔离；改密、会话设备管理待实现 |
| 数据导入导出 | 页面入口已完成；账目页 CSV 导出已可用，通用导入待实现                        |
| 备份与恢复   | 页面入口已完成；Docker Compose 命令行备份已可用，网页恢复流程待实现          |
| 审计日志     | 后端已记录关键操作，查看界面待实现                                           |

## 10. 数据库和审计

### 10.1 PostgreSQL 数据模型

数据库保存：

- 账本。
- 账目。
- 项目字典。
- 分类及合并关系。
- 账户，含往来对象。
- 人情往来记录与每次结算。
- 预算。
- 标签。
- AI 模型配置。
- AI 对话、消息和待确认操作。
- 审计日志。
- 用户、密码哈希和可吊销登录会话。

### 10.2 审计日志

当前会记录：

- 新增账目。
- 修改账目。
- 删除账目。
- 批量分类。
- SQLite 数据导入。
- 分类修改和移动。
- 一级分类重命名。
- 分类合并。
- 分类永久删除。
- 一级分类永久删除。

审计日志目前只能通过数据库读取，网页查看页面尚未实现。

### 10.3 数据初始化

- 第一次启动时自动创建默认账本和名为「默认账户」的现金账户，但不强制设为默认付款账户。
- 数据库为空且未禁用种子数据时，可以导入初始账目 JSON。
- 已有账目时不会重复导入初始数据。
- 支持通过脚本把旧 SQLite 数据迁移到 PostgreSQL。

## 11. Docker 部署和持久化

### 11.1 Compose 服务

| 服务       | 功能                                      |
| ---------- | ----------------------------------------- |
| `web`      | NestJS API、React 静态页面和 Pi SDK Agent |
| `postgres` | PostgreSQL 17 数据库                      |
| `backup`   | 一次性 PostgreSQL 备份工具，不常驻运行    |

### 11.2 持久化位置

| 数据                    | 容器位置                   | 宿主机位置                                |
| ----------------------- | -------------------------- | ----------------------------------------- |
| PostgreSQL 全部业务数据 | `/var/lib/postgresql/data` | 固定命名卷 `qing-zhang-postgres-data`     |
| AI 运行文件             | `/app/data`                | `QING_ZHANG_DATA_DIR`，默认 `./data`      |
| 数据库备份              | `/backups`                 | `QING_ZHANG_BACKUP_DIR`，默认 `./backups` |

更新或重建 `web` 容器不会删除上述数据。

禁止在需要保留数据时执行：

```bash
docker compose down -v
docker volume rm qing-zhang-postgres-data
docker system prune --volumes
```

### 11.3 启动和升级

构建并启动：

```bash
docker compose up -d --build
```

加载已经打包的镜像后启动：

```bash
docker load -i qing-zhang-stack_2.0.0_linux-amd64.tar
docker compose up -d --no-build
```

Web 容器每次启动会：

1. 修正 `/app/data` 和 Pi 目录权限。
2. 降权为普通 `node` 用户。
3. 执行 Prisma 版本化数据库迁移。
4. 启动 NestJS 服务。

### 11.4 数据库备份

创建备份：

```bash
docker compose run --rm backup
```

备份特点：

- 使用 PostgreSQL 自定义格式。
- 文件名带生成时间。
- 默认保存到宿主机 `./backups`。
- 可使用 `pg_restore -l` 校验备份目录。
- 镜像 tar 包不包含数据库卷；跨机器迁移必须同时携带数据库备份、`data` 和 `pi` 目录。

## 12. 健康检查和错误处理

- PostgreSQL 使用 `pg_isready` 健康检查。
- Web 容器通过 `/api/health` 检查服务。
- Web 只有在 PostgreSQL 健康后才启动。
- 健康接口无需登录，只返回服务和数据库连通状态，不暴露用户模型信息。
- NestJS 使用统一异常过滤器返回 API 错误。
- 表单和接口错误通过页面通知展示。
- 使用 Helmet 设置常规 HTTP 安全响应头。
- 支持进程优雅停止和容器停止宽限期。

系统已具备个人账户隔离，但仍建议通过 HTTPS 反向代理对外提供服务，并在 HTTPS 部署时设置 `COOKIE_SECURE=1`。

## 13. API 功能概览

### 13.1 认证、健康和字典

| 方法   | 地址                 | 功能                         |
| ------ | -------------------- | ---------------------------- |
| `GET`  | `/api/auth/status`   | 公开检查认证功能状态         |
| `POST` | `/api/auth/register` | 注册并创建长期登录会话       |
| `POST` | `/api/auth/login`    | 登录并创建长期登录会话       |
| `GET`  | `/api/auth/me`       | 获取当前登录用户             |
| `POST` | `/api/auth/logout`   | 吊销当前会话并清除 Cookie    |
| `GET`  | `/api/health`        | 服务和数据库连通状态         |
| `GET`  | `/api/dictionaries`  | 启用中的项目、分类和账户字典 |

### 13.2 账目

| 方法     | 地址                                | 功能                     |
| -------- | ----------------------------------- | ------------------------ |
| `GET`    | `/api/transactions`                 | 最近账目或服务端分页查询 |
| `POST`   | `/api/transactions`                 | 新增一笔或多笔账目       |
| `PUT`    | `/api/transactions/:id`             | 修改账目                 |
| `DELETE` | `/api/transactions/:id`             | 删除账目                 |
| `POST`   | `/api/transactions/bulk-categorize` | 批量分类                 |

分页查询支持 `page`、`pageSize`、`month`、`query`、`category1`、`category2`、`direction`、`accountId`、`sortBy` 和 `sortOrder` 参数。`accountId=none` 筛选未记账户的账目；新增时 `accountId` 可省略（有默认付款账户则使用它，否则不记账户）、设为真实账户编号，或设为 `none` 表示不记账户。

### 13.3 统计

| 方法  | 地址                   | 功能                                 |
| ----- | ---------------------- | ------------------------------------ |
| `GET` | `/api/dashboard`       | 获取锚点日期对应的日、周、月、年统计 |
| `GET` | `/api/dashboard/range` | 获取自定义日期范围统计               |

### 13.4 AI

| 方法     | 地址                                    | 功能                           |
| -------- | --------------------------------------- | ------------------------------ |
| `GET`    | `/api/ai/settings`                      | 获取脱敏后的当前模型和配置列表 |
| `PUT`    | `/api/ai/settings`                      | 兼容更新当前模型配置           |
| `PUT`    | `/api/ai/settings/prompt`               | 保存当前用户的助手提示词       |
| `POST`   | `/api/ai/settings/profiles`             | 新增模型配置                   |
| `PUT`    | `/api/ai/settings/profiles/:id`         | 修改模型配置                   |
| `POST`   | `/api/ai/settings/profiles/:id/default` | 设置默认模型                   |
| `DELETE` | `/api/ai/settings/profiles/:id`         | 删除非默认模型配置             |
| `GET`    | `/api/ai/conversations`                 | 获取持久化会话列表             |
| `POST`   | `/api/ai/conversations`                 | 新建持久化会话                 |
| `GET`    | `/api/ai/conversations/:id`             | 获取会话消息和待确认操作       |
| `POST`   | `/api/ai/command`                       | 发送一轮多轮 Agent 消息        |
| `POST`   | `/api/ai/execute`                       | 执行用户已确认的待确认操作     |
| `POST`   | `/api/ai/conversations/:id/outcome`     | 告知 Agent 操作已确认或取消    |
| `DELETE` | `/api/ai/conversations/:id`             | 删除持久化会话及其消息         |

### 13.5 基础资料和分类生命周期

| 方法     | 地址                                        | 功能                                         |
| -------- | ------------------------------------------- | -------------------------------------------- |
| `GET`    | `/api/management`                           | 获取账户、项目、分类、预算、标签和分类使用量 |
| `POST`   | `/api/management/:type`                     | 新增账户、项目、分类、预算或标签             |
| `PATCH`  | `/api/management/:type/:id/enabled`         | 启用或停用基础资料                           |
| `PATCH`  | `/api/management/categories/:id`            | 重命名或移动二级分类                         |
| `POST`   | `/api/management/categories/:id/merge`      | 合并分类                                     |
| `DELETE` | `/api/management/categories/:id`            | 安全永久删除无引用分类                       |
| `PATCH`  | `/api/management/categories/primary/rename` | 重命名一级分类及关联数据                     |
| `DELETE` | `/api/management/categories/primary/:name`  | 安全永久删除无引用一级分类                   |
| `GET`    | `/api/management/budgets?month=YYYY-MM`     | 获取预算执行情况                             |
| `PATCH`  | `/api/management/budgets/:id`               | 修改预算                                     |
| `DELETE` | `/api/management/budgets/:id`               | 删除预算                                     |
| `POST`   | `/api/management/budgets/copy-previous`     | 复制上月预算                                 |

### 13.6 资产与负债

| 方法     | 地址                                                        | 功能                               |
| -------- | ----------------------------------------------------------- | ---------------------------------- |
| `GET`    | `/api/finance`                                              | 获取资产、负债和账户可用额度       |
| `POST`   | `/api/finance/accounts`                                     | 新增资产或信用/贷款账户            |
| `PATCH`  | `/api/finance/accounts/:id`                                 | 修改账户、期初额度或设置/取消默认账户 |
| `POST`   | `/api/finance/accounts/:id/reconcile`                       | 按实际可用额度校准账户             |
| `DELETE` | `/api/finance/accounts/:id`                                 | 删除零余额且无历史的账户           |
| `POST`   | `/api/finance/transfers`                                    | 记录账户间资金转移                 |
| `DELETE` | `/api/finance/transfers/:id`                                | 撤销账户转账                       |
| `DELETE` | `/api/finance/adjustments/:id`                              | 撤销额度调整                       |
| `POST`   | `/api/finance/repayments`                                   | 本金转账加可选利息/手续费支出      |

### 13.7 人情往来

| 方法     | 地址                          | 功能                                     |
| -------- | ----------------------------- | ---------------------------------------- |
| `GET`    | `/api/lending`                | 获取往来汇总、往来对象和待结清明细       |
| `GET`    | `/api/lending/reminders`      | 获取逾期与临近到期的往来                 |
| `GET`    | `/api/lending/contacts/:id`   | 获取单个往来对象的明细与资金流水         |
| `POST`   | `/api/lending/contacts`       | 新增往来对象                             |
| `PATCH`  | `/api/lending/contacts/:id`   | 改名或启停往来对象                       |
| `DELETE` | `/api/lending/contacts/:id`   | 删除没有历史记录的往来对象               |
| `POST`   | `/api/lending/entries`        | 记一笔垫付、代付或借入                   |
| `DELETE` | `/api/lending/entries/:id`    | 撤销往来并连带撤销资金移动与关联账目     |
| `POST`   | `/api/lending/settlements`    | 按笔或按人结算收款与还钱                 |
| `DELETE` | `/api/lending/settlements/:id`| 撤销一次结算                             |

### 13.8 消费标签

| 方法     | 地址                                                   | 功能                       |
| -------- | ------------------------------------------------------ | -------------------------- |
| `GET`    | `/api/tags?scope=month\|year&period=YYYY-MM\|YYYY`     | 获取月度或年度标签概览     |
| `GET`    | `/api/tags/:id?scope=month\|year&period=YYYY-MM\|YYYY` | 获取单个标签消费分析与账单 |
| `POST`   | `/api/tags`                                            | 新增标签                   |
| `PATCH`  | `/api/tags/:id`                                        | 改名、换色、启用或停用标签 |
| `DELETE` | `/api/tags/:id`                                        | 删除标签并解除历史账目关联 |

## 14. 技术栈

- 前端：React 19、TypeScript、Vite。
- UI：Ant Design 6、Ant Design Icons、统一主题令牌和响应式应用框架。
- 数据请求：TanStack Query。
- 表格与表单：Ant Design Table、Form、Drawer、Select、DatePicker。
- 图表：Ant Design Charts。
- AI 对话界面：Ant Design X，回答使用 Markdown 渲染。
- 反馈：Ant Design App 消息上下文、Modal、Dropdown 和图标组件。
- 后端：NestJS 模块化单体。
- 数据库：PostgreSQL 17、Prisma ORM、版本化迁移。
- AI：`@earendil-works/pi-coding-agent`。
- 部署：Docker、Docker Compose。

## 15. 当前尚未实现的功能

为避免把界面入口误认为已经完成，以下功能目前尚未实现：

- 多账本创建和切换。
- 修改密码、找回密码和登录设备/会话管理。
- 标签绑定账目。
- 项目重命名、合并和删除。
- 账户删除，以及修改账户类型。
- 通用数据导入向导。
- 网页端数据库备份恢复操作。
- 网页端审计日志浏览和筛选。
- 个人设置和安全设置的实际后端服务。
- 自动定时备份；当前提供手动 Compose 备份命令。
